fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
psycopg2-binary==2.9.9
pymongo[srv]>=4.0.0

# IMPORTANT : de nombreux modules (faiss, torch, scikit-learn...) ne sont pas
# encore compatibles avec NumPy 2.x, on force donc NumPy 1.x.
numpy<2.0.0

# Versions stables pour la stack NLP/RAG (évite les erreurs de compatibilité)
sentence-transformers==2.2.2
transformers==4.30.2
huggingface-hub==0.24.0

# Validation d'email pour les modèles Pydantic
email-validator

faiss-cpu
openpyxl>=3.1.0
pandas>=2.0.0
redis>=5.0.0

# AJOUTEZ CES LIGNES POUR PyTorch
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0
--extra-index-url https://download.pytorch.org/whl/cpu

# OU pour CPU seulement (plus léger)
# torch==2.1.0+cpu --index-url https://download.pytorch.org/whl/cpu

# Speech-to-Text (Whisper) pour la voix
openai-whisper